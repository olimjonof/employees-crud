# employees-crud
A small full stack application that allows users to add, edit, delete, register and export employees. The frontend is a Vue 3 SPA with Vuetify 3 and TypeScript. The backend is Laravel. It downloads an Excel (.xlsx) file for export. The application works with a 3-step installation and is served at http://localhost:8000 using the php artisan servce.
